# Sample Agent

This is a minimal compliant submission. It is not competitive; it exists to show the required file structure and output format.

Run it through the evaluator:

```bash
python3 ../../evaluator/run_submission.py .
```
